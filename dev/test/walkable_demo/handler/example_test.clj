(ns walkable-demo.handler.example-test
  (:require [clojure.test :refer :all]
            [integrant.core :as ig]
            [ring.mock.request :as mock]
            [walkable-demo.handler.example :as example]))
